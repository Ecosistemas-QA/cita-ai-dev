export const metadata = {
  title: 'Iniciar Sesión - CITA AI',
  description: 'Inicia sesión en tu cuenta',
}

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
